import React from 'react';

const AddDoctor = () => {
    return (
        <div>
            {/* <h2>Add Doctor</h2>
            {email && <Alert sx={{ width: { xs: "95%", md: "75%", lg: "25%" }, my: 3 }} severity="success">You Are Successfully Registered As Admin</Alert>}
            <form onSubmit={handleAdminSubmit}>
                <TextField
                    sx={{ width: { xs: "95%", md: "75%", lg: "25%" }, my: 3 }}
                    onBlur={handleOnBlur}
                    type="email"
                    name='email'
                    id="outlined-basic"
                    label="Email"
                    variant="outlined" />
                <br />
                <Button sx={{ width: { xs: "95%", md: "75%", lg: "25%" } }} type="submit" variant="contained">Make Admin</Button>
            </form> */}
        </div>
    );
};

export default AddDoctor;